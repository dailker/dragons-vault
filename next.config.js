/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    esmExternals: false
  },
  webpack: (config) => {
    config.resolve.fallback = {
      ...config.resolve.fallback,
      crypto: false,
      stream: false,
      buffer: require.resolve('buffer')
    };
    return config;
  }
};

module.exports = nextConfig;
rCrypto.multiLayerEncrypt

h (error) {
        errors.push(`MongoDB co

github.com/codehubbers/dragons-vault/issues) • [Securit

     onClick={() => setIsRegister(true)}
          >
   

27017/dragonsVault');
  }

  if (req.method ===

../lib/env-check';

export defa

alhost:27017/dragonsVault');
  }

  if (req.method === '
