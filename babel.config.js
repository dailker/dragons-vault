module.exports = {
  presets: [
    ['@babel/preset-env', {
      targets: {
        node: 'current'
      }
    }],
    '@babel/preset-react'
  ]
};
rchQuery.trim()) {
      searchUsers();
    } else {


      
      let chat = await Chat.findOne({ chatId

rs?q=${encodeURIComponent(searchQuery)

E 
AUTHORS 

odel('User', userSchema);

export defa


      if (!firstLayer) throw new Erro

tic init() {
    const savedTheme = localStor

ent.setAttribu
